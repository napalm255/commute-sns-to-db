---
AWSTemplateFormatVersion: 2010-09-09
Transform:
  - AWS::Serverless-2016-10-31
  - AWS::CodeStar

Parameters:
  ProjectId:
    Type: String
    Description: CodeStar projectId used to associate new resources to team members

Resources:
  SnsToDb:
    Type: AWS::Serverless::Function
    Properties:
      Handler: index.handler
      Runtime: python2.7
      Timeout: 30
      Policies:
        - AmazonSNSFullAccess
#  SnsToDbSubscription:
#    Type: AWS::SNS:Subscription
#    Properties:
#      Endpoint:
#        Fn::GetAtt: ["SnsToDb", "Arn"]
#      Protocol: SNS
#      TopicArn: !Sub arn:aws:sns:${AWS::Region}:${AWS::AccountId}:commute_traffic
#    DependsOn: "SnsToDb"
